name: Sandbox live nightly

# Pull requests run a provider's live tests only when they change something it depends on
# (see `scripts/sandbox_live_changes.py`). A provider can still break with no change here:
# its service changes behavior, or its API rejects a request it used to accept. This runs
# every provider daily against `main` to catch that, and opens an issue when a run fails.
#
# A manual run tests any providers on any ref. To run the live tests for a fork's pull
# request, read its diff first, then pass `refs/pull/<number>/head` and leave "Use workflow
# from" on `main`: the workflow definition then comes from `main`, and only the tested code
# comes from the fork.

on:
  schedule:
    - cron: '17 4 * * *'
  workflow_dispatch:
    inputs:
      providers:
        description: 'JSON list of providers, for example ["modal"], or `all`.'
        type: string
        default: all
      ref:
        description: 'Ref to test, for example refs/pull/123/head. Empty tests the selected branch.'
        type: string
        default: ''

permissions:
  contents: read

concurrency:
  group: sandbox-live-nightly-${{ inputs.ref || github.ref }}
  cancel-in-progress: false

jobs:
  live:
    uses: ./.github/workflows/sandbox-live.yml
    with:
      providers: ${{ inputs.providers || 'all' }}
      ref: ${{ inputs.ref || '' }}
    secrets:
      MODAL_TOKEN_ID: ${{ secrets.MODAL_TOKEN_ID }}
      MODAL_TOKEN_SECRET: ${{ secrets.MODAL_TOKEN_SECRET }}
      E2B_API_KEY: ${{ secrets.E2B_API_KEY }}
      DAYTONA_API_KEY: ${{ secrets.DAYTONA_API_KEY }}
      SPRITE_TOKEN: ${{ secrets.SPRITE_TOKEN }}

  report:
    needs: [live]
    # Only the scheduled run files an issue; a manual run's result is on its own page.
    if: ${{ failure() && github.event_name == 'schedule' }}
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      issues: write
    steps:
      - name: Open or update the nightly failure issue
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          RUN_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
        run: |
          title='Sandbox live nightly is failing'
          # `--search` matches words rather than the phrase, so it narrows the listing and
          # the exact title is compared here.
          number="$(gh issue list --state open --search "${title} in:title" \
            --json number,title --jq ".[] | select(.title == \"${title}\") | .number" | head -n 1)"
          if [ -n "$number" ]; then
            gh issue comment "$number" --body "Failed again: ${RUN_URL}"
            exit 0
          fi

          cat > "${RUNNER_TEMP}/nightly.md" <<EOF
          The scheduled sandbox live tests failed on \`main\`: ${RUN_URL}

          Each provider runs as its own job, so the run page shows which provider failed. A failure with no recent change to that provider's code usually means the provider's service changed or had an outage; a pull request touching that provider will fail the same way until this is fixed.

          Close this issue once a nightly run passes.
          EOF
          gh issue create --title "${title}" --body-file "${RUNNER_TEMP}/nightly.md"
